// Multi-area switcher removed — each empresa atende a uma única área.
// Componente mantido como no-op para evitar regressões em imports legados.
export function AreaSwitcher(_: { variant?: "pill" | "compact"; className?: string }) {
  return null;
}
